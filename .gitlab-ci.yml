---
image: "buluma/github-action-molecule:4.0.6"

services:
  - docker:dind

variables:
  DOCKER_HOST: "tcp://docker:2375"
  PY_COLORS: 1

molecule:
  stage: build
  allow_failure: true
  script:
    - image=${image} tag=${tag} molecule -v test
  retry: 1
  parallel:
    matrix:
      - image: "enterpriselinux"
        tag: "7"
      - image: "centos-systemd"
        tag: "7"
      - image: "centos-systemd"
        tag: "latest"
      - image: "centos-systemd"
        tag: "systemd"
      - image: "centos"
        tag: "centos8"
      - image: "centos"
        tag: "centos7"

testing:
  stage: test
  needs: []
  script:
    - image=${image} tag=${tag} molecule -v test
  rules:
    - if: $CI_COMMIT_REF_NAME == "testing"
  retry: 1
  parallel:
    matrix:
      - image: "enterpriselinux"
        tag: "7"
      - image: "centos-systemd"
        tag: "7"
      - image: "centos-systemd"
        tag: "latest"
      - image: "centos-systemd"
        tag: "systemd"
      - image: "centos"
        tag: "centos8"
      - image: "centos"
        tag: "centos7"

galaxy:
  stage: deploy
  needs: []
  script:
    - ansible-galaxy role info buluma.redmine

role_testing:
  stage: test
  needs: []
  script:
    - image=${image} tag=${tag} ansible-galaxy install buluma.redmine
  retry: 1
  parallel:
    matrix:
      - image: "enterpriselinux"
        tag: "7"
      - image: "centos-systemd"
        tag: "7"
      - image: "centos-systemd"
        tag: "latest"
      - image: "centos-systemd"
        tag: "systemd"

release_job:
  stage: .post
  needs: [molecule]
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  rules:
    - if: $CI_COMMIT_TAG
  script:
    - echo "Running the release job."
  release:
    tag_name: $CI_COMMIT_TAG
    name: 'Release $CI_COMMIT_TAG'
    description: 'Release created using the release-cli.'
  artifacts:
    name: "$CI_JOB_STAGE-$CI_COMMIT_REF_NAME"
    paths:
      - /
    expire_in: 1 day
